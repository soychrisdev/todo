import React from 'react'

function TodoList(params : any) {

    // console.log(params)
  return (
    <div>TodoList
        <div>
        {/* {todos.map((todo: any) => {
          (
            <div key={todo.id}>
              <h1>{todo.title}</h1>
            </div>
          )
        })} */}
        </div>
    </div>
  )
}

export default TodoList